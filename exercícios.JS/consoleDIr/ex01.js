function criarConsoleDir() {
    const nome = 10;
    return (nome);
}

function criarConsoleLog() {
    const nome = 'Rafael';
    return (nome);
}

console.log('console.dir, resultara em um console mais detalhado sobre aquele objeto: ' + criarConsoleDir());
console.log('console.log, resultara em um console mais simples sobre aquele objeto: ' + criarConsoleLog());	
